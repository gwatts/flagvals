/*
Package flagvals implements some useful types for use with the standard
library flag package.

These types implements the flag.Value interface, so can be used with
flag.Var or with CLI libraries such as github.com/jawher/mow.cli
*/
package flagvals
